#include "main.h"
#include <unistd.h>
#include <stdio.h>
/**
 * _strcat - strcat
 *
 * @src: variable
 * @dest: variable
 * Return: return the printed
 */
char *_strncat(char *dest, char *src)
{
	int i = 0, dest_len = 0; int src_len = 0;

	while (dest[dest_len] != '\0')
        dest_len++;
	
	while (src[src_len]) != '\0')
	src_len++
	for (i = )	
}


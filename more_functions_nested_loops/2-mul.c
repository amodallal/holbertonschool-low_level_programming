#include "main.h"
#include <unistd.h>
#include <stdio.h>
/**
 * mul - multiply two numbers
 * @a: - first number
 * @b: - seconf number
 * Return: return the result
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
